#include "../includes/memory_handler.h"

void *ft_realloc(void *ptr, size_t size) {
    printf("\n********** realloc **********\n");
    if (ptr) {
        t_block *block = (t_block *)ptr - 1;

        // if ()
    }
}
